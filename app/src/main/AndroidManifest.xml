<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Web augmentation tools fetch directly from the device IP. No proxy, no AI API. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Attaching an image from the gallery for the multimodal responder. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Direct filesystem access to the user-placed .litertlm model files.
         LiteRT-LM requires a real path; this is the access strategy chosen for v1. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:name=".SmallTalkApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmallTalk">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.SmallTalk">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
